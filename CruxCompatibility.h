#pragma once

// This code can be dropped into a blank Unreal C++ project whose primary
// module is not named "Crux". Unreal normally generates a PROJECT_API macro
// for each module (for example, MYGAME_API). Define CRUX_API as empty when the
// original Crux module macro is not available so the sample classes remain
// project-name agnostic.
#ifndef CRUX_API
#define CRUX_API
#endif
