# Core ML
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
scipy>=1.12.0

# Point cloud I/O and processing
laspy>=2.5.0          # read/write .las / .laz files
lazrs>=0.5.0          # laz decompression backend for laspy
open3d>=0.18.0        # visualization, normal estimation, voxel downsampling

# Training utilities
pytorch-lightning>=2.2.0
torchmetrics>=1.3.0
wandb>=0.16.0         # experiment tracking
rich>=13.0.0          # pretty progress bars

# Data / geometry
scikit-learn>=1.4.0   # KNN for local density, active learning
shapely>=2.0.0        # polygon aggregation for UE5 export
pyproj>=3.6.0         # GPS coordinate transforms

# Config
omegaconf>=2.3.0
hydra-core>=1.3.0

# Labeling tooling (optional, for active learning export)
pandas>=2.2.0

# Dev / test
pytest>=8.0.0
black>=24.0.0
ruff>=0.3.0
